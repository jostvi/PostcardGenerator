<!DOCTYPE html>
<html lang="sv">

<head>
    <title>Postcard generator</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1>You accessed this page via a GET request</h1>
    <form id="user-input-text" method="GET" action="/testuserinput">
        <label for="userinput">Ange text på vykortet</label>
        <input type="text" maxlength="300" id="userinput" name="sample-text"><br>
        <input type="submit" id="submit-btn" value="OK">
    </form>

    <p>ID passed: </p>
    <p id="preview-text">{{ output }}</p>



{{!-- 


        $('#submit-btn').click(function(){
            document.getElementById("#preview-text").innerHTML() = document.getElementById("#userinput").innerHTML()

        // Hämtar adressen till bilden som användaren klickade på
        var  = $(this).attr("src");
        // Lägger till en större version av bilden (dold)
        $("body").prepend("<img src='" + src + "' style='position: fixed; width: 30%; top: 10%; left: 35%; z-index: 6; border: 10px solid #fff; background-color: white; box-shadow: 0 0 20px #000; display: none;' id='imageModal'>")
        // Lägger till en svart bakgrund (dold)
        $("body").prepend("<div id='backgroundModal' style='top: 0; bottom: 0; left: 0; right: 0; position: fixed; background-color: rgba(0,0,0,0.5); display:none; z-index: 5;'></div>");
        // Visar bakgrunden genom en animation
        $("#backgroundModal").fadeIn(500);
        // Visar bilden genom en animation
        $("#imageModal").fadeIn(500); --}} 

    <img src='https://res.cloudinary.com/dqhlic2nx/image/upload/v1578649020/puaeieyjswx4fdgeof9g.jpg' alt='Vykort'
        class="bigImage">
    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS, then local script -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"
        integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo"
        crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
        crossorigin="anonymous"></script>
    <script type="text/javascript" src="/scripts/script.js"></script>
 
    <script>

        $("input#submit-btn").on("click", function () {
        var userinput= document.getElementById("#userinput").attr("name")
        $("p#preview-text").replaceWith(userInput);
        })

    </script>
</body>

</html>